export interface Data {
  id: {
    name: string;
    value: string;
  };
  name: {
    first: string;
    last: string;
  };
  dob: {
    date: string;
  };
  location: {
    city: string;
  };
  email: string;
  phone: string;
  picture: {
    large: string;
  };
  nat: string;
}
